name: Format G8 branch

on:
  push:
    branches:
      - feat/mk1-g8-inventory-rack-elevation

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    if: github.actor != 'github-actions[bot]'
    steps:
      - uses: actions/checkout@v7
        with:
          ref: feat/mk1-g8-inventory-rack-elevation
      - uses: actions/setup-node@v5
        with:
          node-version: 22
          cache: npm
      - run: npm ci --no-audit --no-fund
      - run: npm run format
      - name: Remove one-shot formatter
        run: rm .github/workflows/format-g8.yml
      - name: Commit formatting
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit -m "style: format G8 implementation"
          git push origin HEAD:feat/mk1-g8-inventory-rack-elevation
