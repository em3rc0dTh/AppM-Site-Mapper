# Legacy Route Inventory

Initial route evidence observed in the legacy repository:

- /login
- /change-password
- /network
- /sites
- /site/[siteId]
- /site/[siteId]/structure/[structureId]
- /site/[siteId]/structure/[structureId]/level/[levelId]/room/[roomId]
- /site/[siteId]/structure/[structureId]/level/[levelId]/room/[roomId]/container/[containerId]
- /settings
- /popup/container
- /popup/device
- /popup/panel
- /api/label
- /api/telemetry/stream

## MK1 rule

This is an inventory, not approval of every legacy route.

Deep-link behavior is a capability to preserve. Exact canonical route nouns cannot be frozen until domain terminology is accepted.
